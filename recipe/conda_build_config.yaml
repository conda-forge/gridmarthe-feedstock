c_compiler:                 # [win]
  - clang                   # [win]
cxx_compiler:               # [win]
  - clang                   # [win]
fortran_compiler:           # [win]
  - flang                   # [win]
# assuming flang is default fortan compiler for windows,
# old = by default, version 5 is installed. Update to 21:
# now, fix version
fortran_compiler_version:   # [win]
  - 21                      # [win]

# # use newer numpy; can be removed once migrated for numpy 2.0
# numpy:
#   - 1.26
#   - 1.26
#   - 1.26
#   - 1.26
